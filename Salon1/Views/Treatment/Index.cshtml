@model IEnumerable<Salon1.Models.Treatment>

@{
    ViewBag.Title = "Index";
}

<h2>Treatments</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TreatmentType.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TreatmentType.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TreatmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TreatmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TreatmentID })
        </td>
    </tr>
}

</table>
